<%= render partial: 'patient', locals: { patient: @patient } %>

<h2>Appointments</h2> <!-- show the patient's appointments -->

<% @appointments.each do |appointment| %>
  <ul>
    <%= render partial: 'appointments/appointment_li', locals: { appointment: appointment } %>
  </ul>
<% end %>
