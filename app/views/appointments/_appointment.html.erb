<%= date_and_time(appointment) %><br>
<%= appointment.location %><br>
<%= appointment.patient.name %><br>
<%= appointment.user.name %><br><br>

<td>
  <%= link_to "Edit", edit_appointment_path(appointment), class: "right-margin-btn btn btn-primary btn-sm" %>
</td>
<td>
  <%= link_to "Delete", appointment, method: :delete, data: { confirm: "Are you sure you would like to delete this appointment?" }, class: "delete-appointment right-margin-btn btn btn-primary btn-sm" %>
</td><br><br>

<h3>Notes</h3>

Create a new note
<%= form_for @appointment_note do |f| %>
  Content: <%= f.text_field :content %>
  <%= f.hidden_field :appointment_id, value: appointment.id %>
  <%= f.submit "Create Note "%>
<% end %>

<div>
  <% appointment.appointment_notes.each do |note| %>
    <%= note.content %> - Created at: <%= %>
  <% end %>
</div>
